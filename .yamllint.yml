---
extends: default

ignore: |
  node_modules/
  dist/

rules:
  document-start:
    present: true
    level: error
  line-length: disable
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
